extends layouts/main.pug

block content
    .row.justify-content-center.mt-5
        .col-md-5
            .card.shadow-lg.border-0
                .card-header.bg-primary.text-white.text-center.p-4
                    h2.mb-0 Create Account
                    p.small.mb-0 Join our community of service seekers
                .card-body.p-5
                    if error
                        .alert.alert-danger= error
                    
                    form(action="/api/auth/signup" method="POST")
                        .mb-3
                            label.form-label Full Name
                            input.form-control(type="text" name="name" required placeholder="John Doe")
                        .mb-3
                            label.form-label Email Address
                            input.form-control(type="email" name="email" required placeholder="john@example.com")
                        .mb-3
                            label.form-label Phone Number
                            input.form-control(type="text" name="phone" required placeholder="+1234567890")
                        .mb-3
                            label.form-label Password
                            input.form-control(type="password" name="password" required placeholder="••••••••")
                            .form-text.small.text-muted Minimum 8 characters recommended.
                        .d-grid.mt-4
                            button.btn.btn-primary.btn-lg(type="submit") Sign Up
                    .text-center.mt-4
                        p.text-muted Already have an account? 
                            a(href="/login") Login here
